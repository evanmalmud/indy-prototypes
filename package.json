{
  "name": "indy-prototypes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Indiana Jones puzzle prototypes — testing three design pillars against one shared toolset",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vitest": "^4.0.0"
  }
}
